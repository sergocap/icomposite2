.ics
  - @ics.each do |ic|
    - if ic.id != @ic.id
      = link_to (image_tag ic.image.url(:small), height: 100, class: 'opacity'), ic_path(ic)
    - else
      = image_tag ic.image.url(:small), height: 100

.ic.main
  = @ic.html.html_safe
